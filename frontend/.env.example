# Leave both unset to run in mock mode (the default — works with no backend).
# Set both to switch the dashboard onto Supabase Realtime.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional. If set, the kill switch POSTs here as well as flipping locally.
# VITE_API_URL=http://localhost:8000
